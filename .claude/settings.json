{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write|NotebookEdit",
        "hooks": [
          {
            "type": "command",
            "command": "jq -r '.tool_input.file_path // .tool_input.notebook_path // empty' | { read -r f; case \"$f\" in *.ex|*.exs|*.heex) cd /Users/dylanglover/Documents/GitHub/ChatSupportApp && mix format \"$f\" ;; esac; } 2>/dev/null || true",
            "statusMessage": "Running mix format..."
          }
        ]
      }
    ]
  }
}
